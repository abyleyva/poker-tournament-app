# Cadena de conexión a tu base de datos Postgres (por ejemplo, la que te da Supabase o Neon).
# Formato: postgres://usuario:password@host:puerto/nombre_basedatos
DATABASE_URL="postgres://usuario:password@host:5432/basedatos"
